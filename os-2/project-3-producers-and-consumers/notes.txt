/*
	If semaphore count is 0, new consumer threads sleep. 
	If semaphore count is > 0, new consumer threads run and decrement. 
	If there are sleeping consumer threads, new producer threads wakes up a sleeping thread. 
	If there aren't sleeping consumer threads, new producer threads increment. 
	If at capacity for count, new producer threads sleep. 

	Every call to wait decrements the count. 

	///
		Run producer
			Loop
				Wait 
				Add item to queue 

		Run consumer
			Loop
				Remove item from queue 
				Post 
	///
*/
